<div class="p-3 mt-5 mb-4 border border-success border-5 rounded-3 mx-5">
    <div class="container-fluid py-5" style="background-image: url('{{ asset('assets/media/bureau.jpg') }}'); background-position: center; -webkit-text-stroke: 1px black;" alt="Room">
        {% if app.user %}
            <h1 class="display-5 fw-bold" style="color:white">Bienvenue, {{ app.user.email }}!</h1>
        {% else %}
            <h1 class="display-5 fw-bold" style="color:white">Bienvenue sur mon ECF</h1>
        {% endif %}
        <h2 class="col-md-8 fs-4" style="color:white">Gérer la logistique de vos salles. Ajouter autant de réservations que vous souhaitez grâce à une interface intuitive.</h2>
        
        {% if app.user %}
            <a class="btn btn-lg btn-success border border-1px" href={{ path('app_rooms') }}>Les salles</a>
          {% else %}
            <div type="hidden">
              <a class="btn btn-lg btn-success border border-1px" href={{ path('app_login') }}>Les salles</a>
            </div>
          {% endif %}
    </div>
</div>