{% extends 'base.html.twig' %}

{% block title %}{{ oneRoom.name }}{% endblock %}

{% block body %}
<h1 class="h3 mb-3 text-center">{{ oneRoom.name }}</h1>
<div class="card card p-4 rounded-3">
	<p>Description :<br>{{ oneRoom.description }}</p>
	<p>Capacité : {{ oneRoom.capacity }} personnes</p>
</div>
            <div class="text-center">
            <br>
            <a class="btn btn-success" href={{ path('app_rooms') }}>Revenir à toutes les notes</a>
            <a class="btn btn-success">Réserver {{ oneRoom.name }}</a>

            </div>
{% endblock %}
